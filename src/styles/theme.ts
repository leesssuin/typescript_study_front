import { DefaultTheme } from "styled-components";

const color = {
  black: "#000000",
  white: "#ffffff"
};

const fontSize = {
  sm: "0.714rem",
  md: "0.857rem",
  base: "1rem",
  lg: "1.143rem"
};

export const theme: DefaultTheme = {
  color,
  fontSize
};
